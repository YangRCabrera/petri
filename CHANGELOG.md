# Changelog

All notable user-facing changes to this project are documented here.
Format loosely follows [Keep a Changelog](https://keepachangelog.com/);
versioning follows [Semantic Versioning](https://semver.org/), scoped to
this app's own users/data rather than a public API.

<!--
Create this file empty, day 0. Add an [Unreleased] entry the first time
something changelog-worthy ships, well before the first tag — don't wait
for a version number to exist before starting the list. Tag and rename
[Unreleased] -> [x.y.z] - YYYY-MM-DD at each release.

Rough rule of thumb for version bumps (adapt to project):
  PATCH — copy/UI/bug fixes, no schema change, no new service functions
  MINOR — additive: new capability, new table/route, widened behavior
          that doesn't break existing callers
  MAJOR — breaks existing data or existing usage
-->

## [Unreleased]

### Added

### Changed

### Fixed
